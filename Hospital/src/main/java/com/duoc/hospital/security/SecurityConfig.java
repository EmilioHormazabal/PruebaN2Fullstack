package com.duoc.hospital.security;

import jakarta.servlet.FilterRegistration;
import org.springframework.boot.web.servlet.FilterRegistrationBean;
import org.springframework.context.annotation.Bean;

//public class SecurityConfig {
    //@Bean
    //public FilterRegistrationBean<SecurityApiKeyFilter> apiKeyFilter(SecurityApiKeyFilter filer){
        //FilterRegistration<SecurityApiKeyFilter> registrationBean = new FilterRegistrationBean<>();
        //registrationBean.setFilter(filer);
        //registrationBean.addUrlPatterns("/api/v1/*");
        //return registrationBean;
    //}
//}
